<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ meta.siteName }}{% if page.url != '/' %} | {{ title }} {% endif %}</title>
  <link rel="stylesheet" href="{{ '/css/style.css' | url }}">
  {% if page.url == '/coverage/' %}<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/ol@v10.6.0/ol.css">{% endif %}
</head>
<body>

  <header>
    <h1> {{ meta.siteName }}{% if page.url != '/' %}  ~  {{ title }}{% endif %} </h1>
  </header>

  {% include "nav.njk" %}

  {{ content | safe }}

  <footer>
    <div>
      <p>&copy; {% year %} {{ meta.siteName }}{% if meta.authorName %} • {{ meta.authorName }}{% endif %}</p>
    </div>
  </footer>
  {% if page.url == '/coverage/' %}{% include "coverage.njk" %}{% endif %}
</body>
</html>